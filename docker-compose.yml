services:
  api:
    image: ghcr.io/tabslate-dev/tabslate-server:latest
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - PORT=8080
      - GIN_MODE=release
    restart: unless-stopped
