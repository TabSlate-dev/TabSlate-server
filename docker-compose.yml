services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      DATABASE_URL: ${DATABASE_URL:-file}
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      GIN_MODE: ${GIN_MODE:-release}
      PORT: "8080"
    restart: unless-stopped
